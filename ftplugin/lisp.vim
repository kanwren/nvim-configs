set lisp
set lispwords+=$vau,$lambda,$macro,$define!,$provide!,$import!,$set,$get,$if,$let,$letrec,$let*,$letrec*,$let-redirect,$let-safe,$when,$unless,$let/cc,for-each,$while,$do-while,$while-then,$for-each
